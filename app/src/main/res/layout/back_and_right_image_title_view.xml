<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/height_top_bar"
    android:paddingRight="10dp"
    android:paddingLeft="10dip"
    android:id="@+id/vRoot"
    android:background="@color/title_background"
     >

    <ImageView
        android:id="@+id/back_image_view_left"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:src="@mipmap/img_back_write"
        android:contentDescription="@string/app_name" />

    <FrameLayout
        android:id="@+id/right_widget"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <ImageView
            android:id="@+id/back_image_view_right"
            android:layout_width="19dp"
            android:layout_height="40dp"
            />

        <TextView
            android:id="@+id/image_txt"
            android:gravity="center"
            android:textColor="@color/color_text_yellow"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:textSize="14dp"
            />
    </FrameLayout>

    <TextView
        android:id="@+id/back_image_view_title"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:layout_toRightOf="@id/back_image_view_left"
        android:layout_toLeftOf="@id/right_widget"
        android:gravity="center"
        android:textColor="@color/white"
        android:textIsSelectable="false"
        android:ellipsize="end"
        android:singleLine="true"
        android:textSize="@dimen/top_bar_center_text_size"/>

</RelativeLayout>