package com.jusfoun.jusfouninquire.ui.fragment;import android.os.Bundle;import android.view.View;import com.jusfoun.jusfouninquire.ui.fragment.BaseInquireFragment;/** * @author henzil * @version create time:2014-9-2_下午11:07:10 * @Description 底层，为了使用当前fragment才做操作的逻辑。封装的一个父类。子类要重写refreshData方法。 *              去处理当前fragment下是否加载什么数据之类的逻辑 */public abstract class BaseViewPagerFragment extends BaseInquireFragment {	protected boolean isInit; // 是否可以开始加载数据	@Override	public void setUserVisibleHint(boolean isVisibleToUser) {		super.setUserVisibleHint(isVisibleToUser);		// 每次切换fragment时调用的方法		if (isVisibleToUser) {			showData();		} else {			setViewHint();		}	}	@Override	public void onViewCreated(View view, Bundle savedInstanceState) {		super.onViewCreated(view, savedInstanceState);		isInit = true;	}	@Override	public void onResume() {		super.onResume();		// 判断当前fragment是否显示		if (getUserVisibleHint()) {			showData();		}	}	/**	 * 初始化数据	 * 	 */	private void showData() {		if (isInit) {			isInit = false;// 加载数据完成			// 加载各种刷新数据			refreshData();		}	}	protected abstract void setViewHint();	protected abstract void refreshData();}